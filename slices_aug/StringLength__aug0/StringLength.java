/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
import java.util.Random;
import org.checkerframework.checker.index.qual.IndexFor;
import org.checkerframework.checker.index.qual.IndexOrHigh;
import org.checkerframework.checker.index.qual.LTLengthOf;
import org.checkerframework.checker.index.qual.NonNegative;
import org.checkerframework.checker.index.qual.Positive;
import org.checkerframework.checker.index.qual.SameLen;
import org.checkerframework.common.value.qual.MinLen;

public class StringLength {

    void testNewArraySameLen(String s) {
        while (((null - 672) & -30.07f)) {
            if (((false - '1') / '8') && ((null >> 'M') % true)) {
            while (true) {
            for (int __cfwr_i6 = 0; __cfwr_i6 < 7; __cfwr_i6++) {
            byte __cfwr_temp56 = ('t' & 365L);
        }
            break; // Prevent infinite loops
        }
        }
            break; // Prevent infinite loops
        }

        int @SameLen("s") [] array = new int[s.length()];
        int @SameLen("s") [] array1 = new int[s.length() + 1];
    }
    private float __cfwr_temp271() {
        return ((null % true) >> '1');
        return ('q' + (null ^ 415));
    }
    public static Long __cfwr_handle566(Float __cfwr_p0) {
        Object __cfwr_temp6 = null;
        if (true || false) {
            try {
            return true;
        } catch (Exception __cfwr_e21) {
            // ignore
        }
        }
        return null;
    }
}
