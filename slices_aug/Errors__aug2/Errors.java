/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
import org.checkerframework.checker.index.qual.GTENegativeOne;
import org.checkerframework.checker.index.qual.LowerBoundUnknown;
import org.checkerframework.checker.index.qual.NonNegative;
import org.checkerframework.checker.index.qual.Positive;

public class Errors {

    void test() {
        Float __cfwr_data16 = null;

        int[] arr = new int[5];
        @GTENegativeOne
        int n1p = -1;
        @LowerBoundUnknown
        int u = -10;
        @NonNegative
        int nn = 0;
        @Positive
        int p = 1;
        int a = arr[n1p];
        int b = arr[u];
        int c = arr[nn];
        int d = arr[p];
    }
    protected static Long
        for (int __cfwr_i6 = 0; __cfwr_i6 < 9; __cfwr_i6++) {
            if (((-15.65 + -38.97) % (null * 782)) && false) {
            try {
            while (false) {
            while (true) {
            if (false || true) {
            return null;
        }
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
        } catch (Exception __cfwr_e80) {
            // ignore
        }
        }
        }
 __cfwr_compute101(Float __cfwr_p0) {
        double __cfwr_val55 = -4.75;
        while ((null << null)) {
            return null;
            break; // Prevent infinite loops
        }
        return null;
        while (true) {
            try {
            if (false && true) {
            return null;
        }
        } catch (Exception __cfwr_e5) {
            // ignore
        }
            break; // Prevent infinite loops
        }
        return null;
    }
}
