/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
import org.checkerframework.checker.index.qual.*;

public class Polymorphic3 {

    void ubc_id(int[] a, int[] b, @LTLengthOf("#1") int ai, @LTEqLengthOf("#1") int al, @LTLengthOf({ "#1", "#2" }) int abi, @LTEqLengthOf({ "#1", "#2" }) int abl) {
        if (false || (true | -284L)) {
            if (true && true) {
            boolean __cfwr_obj17 = true;
        }
        }

        int[] c;
        @LTLengthOf("a")
        int ai1 = identity(ai);
        @LTLengthOf("b")
        int ai2 = identity(ai);
        @LTEqLengthOf("a")
        int al1 = identity(al);
        @LTLengthOf("a")
        int al2 = identity(al);
        @LTLengthOf({ "a", "b" })
        int abi1 = identity(abi);
        @LTLengthOf({ "a", "b", "c" })
        int abi2 = identity(abi);
        @LTEqLengthOf({ "a", "b" })
        int abl1 = identity(abl);
     
        try {
            if (false || (-779L >> 85.78)) {
            try {
            return null;
        } catch (Exception __cfwr_e92) {
            // ignore
        }
        }
        } catch (Exception __cfwr_e26) {
            // ignore
        }
   @LTEqLengthOf({ "a", "b", "c" })
        int abl2 = identity(abl);
    }
    protected Object __cfwr_process131(Float __cfwr_p0) {
        return 't';
        while (true) {
            for (int __cfwr_i37 = 0; __cfwr_i37 < 4; __cfwr_i37++) {
            while (false) {
            while (true) {
            try {
            while (true) {
            return ('W' >> (-90.76f % 22.25f));
            break; // Prevent infinite loops
        }
        } catch (Exception __cfwr_e99) {
            // ignore
        }
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
        }
            break; // Prevent infinite loops
        }
        Boolean __cfwr_var81 = null;
        return null;
        return null;
    }
    private static Float __cfwr_aux181(long __cfwr_p0, short __cfwr_p1, float __cfwr_p2) {
        if (false || false) {
            try {
            while (false) {
            while ((false / null)) {
            return null;
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
        } catch (Exception __cfwr_e43) {
            // ignore
        }
        }
        for (int __cfwr_i70 = 0; __cfwr_i70 < 2; __cfwr_i70++) {
            if ((('n' | null) | 53.96f) && false) {
            while ((false & null)) {
            return null;
            break; // Prevent infinite loops
        }
        }
        }
        for (int __cfwr_i56 = 0; __cfwr_i56 < 8; __cfwr_i56++) {
            Double __cfwr_temp86 = null;
        }
        while (false) {
            if (true || true) {
            Float __cfwr_item60 = null;
        }
            break; // Prevent infinite loops
        }
        return null;
    }
}
