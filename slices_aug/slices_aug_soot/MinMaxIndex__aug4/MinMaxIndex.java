/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
import org.checkerframework.checker.index.qual.IndexFor;
import org.checkerframework.checker.index.qual.IndexOrHigh;

public class MinMaxIndex {

    void twoSequences(String str1, String str2, @IndexFor("#1") int i1, @IndexFor("#2") int i2) {
        for (int __cfwr_i94 = 0; __cfwr_i94 < 1; __cfwr_i94++) {
            char __cfwr_entry89 = 'p';
        }

        str1.charAt(Math.max(i1, i2));
        str1.charAt(Math.min(i1, i2));
    }
    private static double __cfwr_compute409() {
        for (int __cfwr_i67 = 0; __cfwr_i67 < 9; __cfwr_i67++) {
            return null;
        }
        double __cfwr_data51 = -91.07;
        return ('T' >> (523 - 31));
        if (true && false) {
            Integer __cfwr_result78 = null;
        }
        return (-154L - -259);
    }
    boolean __cfwr_util372(Long __cfwr_p0) {
        if (true && true) {
            while ((null / (641L >> 65.73f))) {
            return null;
            break; // Prevent infinite loops
        }
        }
        while (false) {
            if ((711L % 922) || (('r' ^ null) & 'B')) {
            while (true) {
            try {
            return ('r' << 56.91);
        } catch (Exception __cfwr_e44) {
            // ignore
        }
            break; // Prevent infinite loops
        }
        }
            break; // Prevent infinite loops
        }
        if ((('X' << -586L) | null) && false) {
            if ((271L * 907) && (347 >> (59.56f + 'I'))) {
            return (false + -85.76);
        }
        }
        return true;
    }
}
