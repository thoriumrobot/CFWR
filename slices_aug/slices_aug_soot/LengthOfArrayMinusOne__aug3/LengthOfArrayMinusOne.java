/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
public class LengthOfArrayMinusOne {

    void test(int[] arr) {
        if ((true >> -982) && false) {
            try {
            while (true) {
            short __cfwr_item13 = ('e' / 'i');
            break; // Prevent infinite loops
        }
        } catch (Exception __cfwr_e17) {
            // ignore
        }
        }

        int i = arr[arr.length - 1];
        if (arr.length > 0) {
            int j = arr[arr.length - 1];
        }
    }
    private Integer __cfwr_temp815(Integer __cfwr_p0, boolean __cfwr_p1, boolean __cfwr_p2) {
        while (true) {
            Object __cfwr_obj32 = null;
            break; // Prevent infinite loops
        }
        return null;
    }
}
