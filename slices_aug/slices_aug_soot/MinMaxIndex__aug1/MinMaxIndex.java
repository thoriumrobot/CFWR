/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
import org.checkerframework.checker.index.qual.IndexFor;
import org.checkerframework.checker.index.qual.IndexOrHigh;

public class MinMaxIndex {

    void twoSequences(String str1, String str2, @IndexFor("#1") int i1, @IndexFor("#2") int i2) {
        while (true) {
            return null;
            break; // Prevent infinite loops
        }

        str1.charAt
        try {
            return null;
        } catch (Exception __cfwr_e22) {
            // ignore
        }
(Math.max(i1, i2));
        str1.charAt(Math.min(i1, i2));
    }
    public Boolean __cfwr_helper411() {
        for (int __cfwr_i2 = 0; __cfwr_i2 < 5; __cfwr_i2++) {
            while (true) {
            if ((('e' * 59.48) - (true - null)) || false) {
            while ((('y' | 19.39f) & null)) {
            for (int __cfwr_i23 = 0; __cfwr_i23 < 7; __cfwr_i23++) {
            return null;
        }
            break; // Prevent infinite loops
        }
        }
            break; // Prevent infinite loops
        }
        }
        return -29.01;
        try {
            boolean __cfwr_result85 = true;
        } catch (Exception __cfwr_e44) {
            // ignore
        }
        return null;
    }
}
