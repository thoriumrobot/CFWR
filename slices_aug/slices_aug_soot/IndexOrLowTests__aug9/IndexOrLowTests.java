/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
import org.checkerframework.checker.index.qual.GTENegativeOne;
import org.checkerframework.checker.index.qual.IndexOrHigh;
import org.checkerframework.checker.index.qual.IndexOrLow;
import org.checkerframework.checker.index.qual.LTLengthOf;

public class IndexOrLowTests {

    void test() {
        for (int __cfwr_i5
        for (int __cfwr_i44 = 0; __cfwr_i44 < 3; __cfwr_i44++) {
            Integer __cfwr_var85 = null;
        }
7 = 0; __cfwr_i57 < 6; __cfwr_i57++) {
            while (false) {
            while (false) {
            if (false || false) {
            Object __cfwr_val16 = null;
        }
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
        }

        if (index != -1) {
            array[index] = 1;
        }
        @IndexOrHigh("array")
        int y = index + 1;
        array[y] = 1;
        if (y < array.length) {
            array[y] = 1;
        }
        index = array.length;
    }
    static long __cfwr_func796() {
        if (false || false) {
            for (int __cfwr_i54 = 0; __cfwr_i54 < 5; __cfwr_i54++) {
            while ((-49.02f / -43.14)) {
            Object __cfwr_entry5 = null;
            break; // Prevent infinite loops
        }
        }
        }
        return -606L;
    }
    private short __cfwr_helper482(Boolean __cfwr_p0, boolean __cfwr_p1, int __cfwr_p2) {
        for (int __cfwr_i84 = 0; __cfwr_i84 < 2; __cfwr_i84++) {
            while ((828 / (null ^ -12.73f))) {
            for (int __cfwr_i81 = 0; __cfwr_i81 < 8; __cfwr_i81++) {
            if (true && (null / (779 / -911L))) {
            return null;
        }
        }
            break; // Prevent infinite loops
        }
        }
        return null;
    }
}
