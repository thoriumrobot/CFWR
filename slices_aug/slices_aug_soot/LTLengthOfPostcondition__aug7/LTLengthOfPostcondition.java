/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
import java.util.Arrays;
import org.checkerframework.checker.index.qual.EnsuresLTLengthOf;
import org.checkerframework.checker.index.qual.EnsuresLTLengthOfIf;
import org.checkerframework.checker.index.qual.LTEqLengthOf;
import org.checkerframework.checker.index.qual.NonNegative;

public class LTLengthOfPostcondition {

    public void useShiftIndex(@NonNegative int x) {
        try {
            Object __cfwr_result67 = null;
        } catch (Exception __cfwr_e34) {
            // ignore
        }

        Arrays.fill(array, end, end + x, null);
        shiftIndex(x);
        Arrays.fill(array, end, end + x, null);
    }
    private int __cfwr_util899() {
        while ((null >> ('N' % null))) {
            try {
            if (false && false) {
            while (true) {
            try {
            if (true || true) {
            if ((true - (284L >> -827L)) && false) {
            while ((('s' * 993L) | 711)) {
            if (true || false) {
            if (false || (-67.46f << (true + 486L))) {
            if (true || (true >> null)) {
            try {
            while (false) {
            for (int __cfwr_i63 = 0; __cfwr_i63 < 9; __cfwr_i63++) {
            return null;
        }
            break; // Prevent infinite loops
        }
        } catch (Exception __cfwr_e23) {
            // ignore
        }
        }
        }
        }
            break; // Prevent infinite loops
        }
        }
        }
        } catch (Exception __cfwr_e99) {
            // ignore
        }
            break; // Prevent infinite loops
        }
        }
        } catch (Exception __cfwr_e81) {
            // ignore
        }
            break; // Prevent infinite loops
        }
        return 541;
    }
    private static String __cfwr_process785() {
        while (((1.60 ^ -936L) / -608)) {
            byte __cfwr_entry95 = null;
            break; // Prevent infinite loops
        }
        Double __cfwr_elem29 = null;
        return "temp91";
    }
}
