/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
import org.checkerframework.checker.index.qual.LTEqLengthOf;
import org.checkerframework.checker.index.qual.LTLengthOf;

public class RefineGTE {

    void testLTL(@LTLengthOf("arr") int test) {
        while (true) {
            while (((null + 40.59f) % true)) {
            return null;
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }

        @LTLengthOf("arr")
        int a = Integer.parseInt("1");
        @LTLengthOf("arr")
        int a3 = Integer.parseInt("3");
        int b = 2;
        if (test >= b) {
            @LTLengthOf("arr")
            int c = b;
        }
        @LTLengthOf("arr")
        int c1 = b;
        if (a >= b) {
            int potato = 7;
        } else {
            @LTLengthOf("arr")
            int d = b;
        }
    }
    protected static double __cfwr_handle64() {
        short __cfwr_temp93 = null;
        if (false || true) {
            return -34.86;
        }
        Object __cfwr_temp63 = null;
        return (767L ^ ('f' / '0'));
    }
    public Boolean __cfwr_handle151() {
        while (true) {
            Object __cfwr_node69 = null;
            break; // Prevent infinite loops
        }
        return null;
        try {
            for (int __cfwr_i20 = 0; __cfwr_i20 < 9; __cfwr_i20++) {
            return null;
        }
        } catch (Exception __cfwr_e40) {
            // ignore
        }
        return null;
    }
}
