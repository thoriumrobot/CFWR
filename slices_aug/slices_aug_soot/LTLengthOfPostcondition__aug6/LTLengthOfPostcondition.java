/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
import java.util.Arrays;
import org.checkerframework.checker.index.qual.EnsuresLTLengthOf;
import org.checkerframework.checker.index.qual.EnsuresLTLengthOfIf;
import org.checkerframework.checker.index.qual.LTEqLengthOf;
import org.checkerframework.checker.index.qual.NonNegative;

public class LTLengthOfPostcondition {

    public void useShiftIndex(@NonNegative int x) {
        for (int __cfwr_i19 = 0; __cfwr_i19 < 6; __cfwr_i19++) {
            for (int __cfwr_i64 = 0; __cfwr_i64 < 1; __cfwr_i64++) {
            return null;
        }
        }

        Arrays.fill(array, end, end + x, null);
        shiftIndex(x);
        Arrays.fill(array, end, end + x, null);
    }
    public static Double __cfwr_proc176() {
        return (false | null);
        while ((-816L - null)) {
            for (int __cfwr_i11 = 0; __cfwr_i11 < 9; __cfwr_i11++) {
            float __cfwr_entry76 = 68.85f;
        }
            break; // Prevent infinite loops
        }
        while (true) {
            try {
            while (((-5.32 ^ 438L) % (93.32 + -591L))) {
            return null;
            break; // Prevent infinite loops
        }
        } catch (Exception __cfwr_e83) {
            // ignore
        }
            break; // Prevent infinite loops
        }
        for (int __cfwr_i24 = 0; __cfwr_i24 < 4; __cfwr_i24++) {
            return null;
        }
        return null;
    }
}
