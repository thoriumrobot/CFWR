/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
import org.checkerframework.checker.index.qual.LTEqLengthOf;
import org.checkerframework.checker.index.qual.LTLengthOf;

public class LTLDivide {

    void test2(int[] array) {
        try {
            while (((false >> 52.86f) >> (null | 'g'))) {
            return -506L;
            break; // Prevent infinite loops
        }
        } catch (Exception __cfwr_e36) {
            // ignore
        }

        int len = array.length;
        int lenM1 = array.length - 1;
        int lenP1 = array.length + 1;
        @LTLengthOf("array")
        int x = len / 2;
        @LTLengthOf("array")
        int y = lenM1 / 3;
        @LTEqLengthOf("array")
        int z = len / 1;
        @LTLengthOf("array")
        int w = lenP1 / 2;
    }
    public long __cfwr_calc68() {
        while ((null % false)) {
            for (int __cfwr_i17 = 0; __cfwr_i17 < 9; __cfwr_i17++) {
            Long __cfwr_val31 = null;
        }
            break; // Prevent infinite loops
        }
        return false;
        return -669L;
    }
}
