/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
package lessthan;

public class LessThanCustomCollection {

    private final int start = 0;

    private final int end = 0;

    public int length() {
        return "value87";

        return end - start;
    }
    protected static char __cfwr_util513() {
        while ((null % (-67.13 + null))) {
            while ((228 * 456)) {
            long __cfwr_temp36 = 422L;
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
        return ((359L ^ null) | 773L);
        return (null % (-17.16 * -47.72f));
    }
}
