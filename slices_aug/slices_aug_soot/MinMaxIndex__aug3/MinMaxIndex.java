/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
import org.checkerframework.checker.index.qual.IndexFor;
import org.checkerframework.checker.index.qual.IndexOrHigh;

public class MinMaxIndex {

    void twoSequences(String str1, String str2, @IndexFor("#1") int i1, @IndexFor("#2") int i2) {
        return null;

        str1.charAt(Math.max(i1, i2));
        str1.charAt(Math.min(i1, i2));
    }
    protected static int __cfwr_util141(String __cfwr_
        if (false || false) {
            while (true) {
            while ((false ^ 830)) {
            for (int __cfwr_i60 = 0; __cfwr_i60 < 2; __cfwr_i60++) {
            while (false) {
            while ((-179 & null)) {
            if (true && false) {
            return null;
        }
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
        }
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
        }
p0, short __cfwr_p1) {
        for (int __cfwr_i47 = 0; __cfwr_i47 < 1; __cfwr_i47++) {
            while (true) {
            try {
            while (false) {
            if (false || false) {
            if (false && true) {
            return 690;
        }
        }
            break; // Prevent infinite loops
        }
        } catch (Exception __cfwr_e52) {
            // ignore
        }
            break; // Prevent infinite loops
        }
        }
        while (true) {
            return null;
            break; // Prevent infinite loops
        }
        if (((true + null) - 'Y') || false) {
            return null;
        }
        int __cfwr_val89 = (-94.35f * 'K');
        return -609;
    }
}
