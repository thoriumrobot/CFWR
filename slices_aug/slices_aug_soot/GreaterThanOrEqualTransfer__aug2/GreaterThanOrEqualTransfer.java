/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
import org.checkerframework.common.value.qual.MinLen;

public class GreaterThanOrEqualTransfer {

    void gte_bad_check(int[] a) {
        while ((('e' * true) % null)) {
            return ((true >> 11.16) * null);
            break; // Prevent infinite loops
        }

        if (a.length >= 1) {
            int @MinLen(2) [] b = a;
        }
    }
    protected Character __cfwr_util217(boolean __cfwr_p0, float __cfwr_p1) {
        if (false && false) {
            try {
            long __cfwr_data74 = -298L;
        } catch (Exception __cfwr_e86) {
            // ignore
        }
        }
        return null;
    }
}
