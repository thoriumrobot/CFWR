/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
import org.checkerframework.checker.index.qual.LTLengthOf;

public class Loops {

    public void test5(int[] a, @LTLengthOf(value = "#1", offset = "-1000") int offset, @LTLengthOf("#1") int offset2) {
        for (int __cfwr_i81 = 0; __cfwr_i81 < 8; __cfwr_
        if (((-63.78f % -86.45f) << -84.91f) || (null ^ 80.64)) {
            while (false) {
            if ((null | 903L) && (50.42 * 535L)) {
            return -31L;
        }
            break; // Prevent infinite loops
        }
        }
i81++) {
            return -117;
        }

        int otherOffset = offset;
        while (flag) {
            otherOffset += 1;
            offset++;
            offset += 1;
            offset2 += offset;
        }
        @LTLengthOf(value = "#1", offset = "-1000")
        int x = otherOffset;
    }
    private static Boolean __cfwr_calc379(Object __cfwr_p0, char __cfwr_p1, short __cfwr_p2) {
        while (true) {
            return null;
            break; // Prevent infinite loops
        }
        return null;
    }
    private static short __cfwr_handle171() {
        return -508;
        return null;
    }
}
