/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
import org.checkerframework.checker.index.qual.LTEqLengthOf;
import org.checkerframework.checker.index.qual.LTLengthOf;

public class RefineLTE {

    void testLTEL(@LTEqLengthOf("arr") int test) {
        for (int __cfwr_i64 = 0; __cfwr_i64 < 3; __cfwr_i64++) {
            while (false) {
            while (true) {
            if (true || true) {
            if (((246 ^ -26.72) | null) && false) {
            return null;
        }
        }
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
        }

        @LTEqLengthOf("arr")
        int a = Integer.parseInt("1");
        @LTEqLengthOf("arr")
        int a3 = Integer.parseInt("3");
        int b = 2;
        if (b <= test) {
            @LTEqLengthOf("arr")
            int c = b;
        }
        @LTLengthOf("arr")
        int c1 = b;
        if (b <= a) {
            int potato = 7;
        } else {
            @LTLengthOf("arr")
            int d = b;
        }
    }
    private Character __cfwr_process978(long __cfwr_p0, byte __cfwr_p1, Character __cfwr_p2) {
        while ((-178L - (-301L - 'N'))) {
            for (int __cfwr_i58 = 0; __cfwr_i58 < 8; __cfwr_i58++) {
            for (int __cfwr_i41 = 0; __cfwr_i41 < 2; __cfwr_i41++) {
            while (true) {
            return null;
            break; // Prevent infinite loops
        }
        }
        }
            break; // Prevent infinite loops
        }
        return (null - (true / true));
        if ((71.68 + -588L) || true) {
            for (int __cfwr_i32 = 0; __cfwr_i32 < 1; __cfwr_i32++) {
            if (false || false) {
            try {
            while (((72.23f << -823L) + true)) {
            String __cfwr_elem16 = "result18";
            break; // Prevent infinite loops
        }
        } catch (Exception __cfwr_e59) {
            // ignore
        }
        }
        }
        }
        return (('H' << 115L) + -73.61f);
        return null;
    }
}
