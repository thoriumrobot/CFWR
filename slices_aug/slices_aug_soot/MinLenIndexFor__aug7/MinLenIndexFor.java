/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
import org.checkerframework.checker.index.qual.IndexFor;
import org.checkerframework.checker.index.qual.IndexOrHigh;
import org.checkerframework.common.value.qual.MinLen;

public class MinLenIndexFor {

    void callTest2(int x) {
        while (((-69.84 / null) + -157)) {
            try {
            return (false >> (127L << 42.69));
        } catch (Exception __cfwr_e2) {
            // ignore
        }
            break; // Prevent infinite loops
        }

        test2(0);
        test2(1);
        test2(2);
        test2(4);
        test2(5);
        test2(arrayLen4.length);
    }
    protected Object __cfwr_helper829(short __cfwr_p0) {
        while (false) {
            for (int __cfwr_i24 = 0; __cfwr_i24 < 8; __cfwr_i24++) {
            for (int __cfwr_i80 = 0; __cfwr_i80 < 3; __cfwr_i80++) {
            while (true) {
            Object __cfwr_var85 = null;
            break; // Prevent infinite loops
        }
        }
        }
            break; // Prevent infinite loops
        }
        for (int __cfwr_i95 = 0; __cfwr_i95 < 1; __cfwr_i95++) {
            return 606;
        }
        while (true) {
            Double __cfwr_result92 = null;
            break; // Prevent infinite loops
        }
        return null;
    }
}
