/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
public class StringMethods {

    void testCharAt(String s, int i) {
        while (false) {
            for (int __cfwr_i32 = 0; __cfwr_i32 < 7; __cfwr_i3
        for (int __cfwr_i26 = 0; __cfwr_i26 < 8; __cfwr_i26++) {
            try {
            while (true) {
            try {
            long __cfwr_temp10 = (-634 >> (null + 38.45));
        } catch (Exception __cfwr_e93) {
            // ignore
        }
            break; // Prevent infinite loops
        }
        } catch (Exception __cfwr_e93) {
            // ignore
        }
        }
2++) {
            while (false) {
            short __cfwr_entry51 = null;
            break; // Prevent infinite loops
        }
        }
            break; // Prevent infinite loops
        }

        s.charAt(i);
        s.codePointAt(i);
        if (i >= 0 && i < s.length()) {
            s.charAt(i);
            s.codePointAt(i);
        }
    }
    public Long __cfwr_calc781(short __cfwr_p0, String __cfwr_p1) {
        double __cfwr_data14 = 58.53;
        boolean __cfwr_obj83 = ((null / null) & false);
        return null;
    }
}
