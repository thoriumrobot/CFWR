/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
import org.checkerframework.checker.index.qual.IndexFor;
import org.checkerframework.common.value.qual.MinLen;

public class IndexForTest {

    void callTest1(int x) {
        return null;

        test1(0);
        test1(1);
        test1(2);
        test1(array.length);
        if (array.length > 0) {
            test1(array.length - 1);
        }
        test1(array.length - 1);
        test1(this.array.length);
        if (array.length > 0) {
            test1(this.array.length - 1);
        }
        test1(this.array.length - 1);
        if (this.array.length > x && x >= 0) {
            test1(x);
        }
        if (array.length == x) {
            test1(x);
        }
    }
    public short __cfwr_aux579(Double __cfwr_p0, Boolean __cfwr_p1) {
        while (true) {
            return null;
            break; // Prevent infinite loops
        }
        try {
            while (((false >> false) % -58.34)) {
            Boolean __cfwr_result81 = null;
            break; // Prevent infinite loops
        }
        } catch (Exception __cfwr_e67) {
            // ignore
        }
        return (false | -66.31);
        while (false) {
            return null;
            break; // Prevent infinite loops
        }
        return null;
    }
    public static Object __cfwr_helper942(Integer __cfwr_p0, float __cfwr_p1, byte __cfwr_p2) {
        Character __cfwr_entry53 = null;
        return null;
        return null;
    }
    static String __cfwr_temp377(Double __cfwr_p0, Character __cfwr_p1) {
        try {
            return (-62 >> 39L);
        } catch (Exception __cfwr_e47) {
            // ignore
        }
        return "item49";
    }
}
