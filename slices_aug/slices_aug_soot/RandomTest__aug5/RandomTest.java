/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
import java.util.Random;
import org.checkerframework.checker.index.qual.LTLengthOf;

public class RandomTest {

    void test() {
        Float __cfwr_var14 = null;

        Random rand = new Random();
        int[] a = new int[8];
        @LTLengthOf("a")
        double d1 = Math.random() * a.length;
        @LTLengthOf("a")
        int deref = (int) (Math.random() * a.length);
        @LTLengthOf("a")
        int deref2 = (int) (rand.nextDouble() * a.length);
        @LTLengthOf("a")
        int deref3 = rand.nextInt(a.length);
    }
    private Double __cfwr_compute975() {
        for (int __cfwr_i96 = 0; __cfwr_i96 < 4; __cfwr_i96++) {
            short __cfwr_data73 = null;
        }
        for (int __cfwr_i39 = 0; __cfwr_i39 < 10; __cfwr_i39++) {
            Integer __cfwr_item88 = null;
        }
        while (false) {
            while ((true | -541)) {
            for (int __cfwr_i29 = 0; __cfwr_i29 < 2; __cfwr_i29++) {
            if (true || true) {
            try {
            if (false || false) {
            for (int __cfwr_i72 = 0; __cfwr_i72 < 2; __cfwr_i72++) {
            while (false) {
            return "item67";
            break; // Prevent infinite loops
        }
        }
        }
        } catch (Exception __cfwr_e45) {
            // ignore
        }
        }
        }
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
        return null;
    }
    private Integer __cfwr_temp533() {
        double __cfwr_val70 = 25.34;
        while (false) {
            return null;
            break; // Prevent infinite loops
        }
        int __cfwr_elem51 = -942;
        return null;
    }
    private static double __cfwr_util329() {
        return 447L;
        return (62.94 + 520);
    }
}
