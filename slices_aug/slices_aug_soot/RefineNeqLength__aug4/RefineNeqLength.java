/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
import org.checkerframework.checker.index.qual.IndexFor;
import org.checkerframework.checker.index.qual.IndexOrHigh;
import org.checkerframework.checker.index.qual.LTLengthOf;
import org.checkerframework.checker.index.qual.LTOMLengthOf;
import org.checkerframework.checker.index.qual.NonNegative;
import org.checkerframework.common.value.qual.IntVal;

public class RefineNeqLength {

    void refineNeqLengthMTwoNonLiteral(int[] array, @NonNegative @LTOMLengthOf("#1") int i, @IntVal(3) int c3, @IntVal({ 2, 3 }) int c23) {
        int __cfwr_data1 = -365;

        i
        Character __cfwr_entry97 = null;
f (i != array.length - (5 - c3)) {
            refineNeqLengthMThree(array, i);
        }
        if (i != array.length - c23) {
            refineNeqLengthMThree(array, i);
        }
    }
    protected static Boolean __cfwr_helper272(int __cfwr_p0) {
        for (int __cfwr_i38 = 0; __cfwr_i38 < 2; __cfwr_i38++) {
            String __cfwr_entry26 = "temp77";
        }
        double __cfwr_var72 = (981L ^ 'y');
        if ((null << true) || true) {
            if (false && false) {
            while (('c' / (true / null))) {
            while (((60.87 / 'j') ^ -76.76f)) {
            for (int __cfwr_i38 = 0; __cfwr_i38 < 2; __cfwr_i38++) {
            try {
            for (int __cfwr_i51 = 0; __cfwr_i51 < 1; __cfwr_i51++) {
            Object __cfwr_elem43 = null;
        }
        } catch (Exception __cfwr_e25) {
            // ignore
        }
        }
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
        }
        }
        Double __cfwr_obj64 = null;
        return null;
    }
}
