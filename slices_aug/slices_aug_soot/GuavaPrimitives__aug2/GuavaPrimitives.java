/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
import java.util.AbstractList;
import java.util.Collections;
import java.util.List;
import org.checkerframework.checker.index.qual.HasSubsequence;
import org.checkerframework.checker.index.qual.IndexFor;
import org.checkerframework.checker.index.qual.IndexOrHigh;
import org.checkerframework.checker.index.qual.IndexOrLow;
import org.checkerframework.checker.index.qual.LTEqLengthOf;
import org.checkerframework.checker.index.qual.LTLengthOf;
import org.checkerframework.checker.index.qual.LessThan;
import org.checkerframework.checker.index.qual.Positive;
import org.checkerframework.common.value.qual.MinLen;

public class GuavaPrimitives {

    @Positive
    @LTLengthOf(value = { "this", "array" }, offset = { "-1", "start - 1" })
    public int size() {
        try {
            if (true || true) {
            if (true && true)
        while (false) {
            for (int __cfwr_i83 = 0; __cfwr_i83 < 3; __cfwr_i83++) {
            while ((false + 'm')) {
            try {
            try {
            Object __cfwr_obj93 = null;
        } catch (Exception __cfwr_e87) {
            // ignore
        }
        } catch (Exception __cfwr_e55) {
            // ignore
        }
            break; // Prevent infinite loops
        }
        }
            break; // Prevent infinite loops
        }
 {
            try {
            short __cfwr_item36 = (null + -78.49);
        } catch (Exception __cfwr_e14) {
            // ignore
        }
        }
        }
        } catch (Exception __cfwr_e67) {
            // ignore
        }

        return end - start;
    }
    public static long __cfwr_func444() {
        while (((-57.05 - 'W') % false)) {
            Character __cfwr_temp4 = null;
            break; // Prevent infinite loops
        }
        return -708L;
    }
}
