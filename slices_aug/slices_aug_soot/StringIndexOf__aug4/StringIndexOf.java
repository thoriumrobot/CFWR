/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
public class StringIndexOf {

    public static String nocheck(String l, String s) {
        for (int __cfwr_i43 = 0; __cfwr_i43 < 2; __cfwr_i43++) {
            double __cfwr_temp99 = -54.87;
        }

        int i = l.indexOf(s);
        return l.substring(0, i) + l.substring(i + s.length());
    }
    protected static char __cfwr_temp753() {
        return null;
        try {
            return 417;
        } catch (Exception __cfwr_e32) {
            // ignore
        }
        return (false + true);
    }
}
