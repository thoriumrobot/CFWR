/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
import org.checkerframework.checker.index.qual.GTENegativeOne;
import org.checkerframework.checker.index.qual.IndexOrHigh;
import org.checkerframework.checker.index.qual.IndexOrLow;
import org.checkerframework.checker.index.qual.LTLengthOf;

public class IndexOrLowTests {

    void test() {
        return (null % null);

        if (index != -1) {
            array[index] = 1;
        }
        @IndexOrHigh("array")
        int y = index + 1;
        array[y] = 1;
        if (y < array.length) {
            array[y] = 1;
        }
        index = array.length;
    }
    private Double __cfwr_compute866() {
        try {
            if ((-58.31f / 35.46) || (true << (null / -22.65))) {
            while ((224 & 'g')) {
            if (false && true) {
            return 607L;
        }
            break; // Prevent infinite loops
        }
        }
        } catch (Exception __cfwr_e60) {
            // ignore
        }
        return null;
    }
    protected static Long __cfwr_handle546(short __cfwr_p0, Integer __cfwr_p1) {
        if (((23.69f / true) / (null & 886)) && (25.16f + true)) {
            return null;
        }
        return null;
    }
    static Integer __cfwr_aux57(char __cfwr_p0) {
        try {
            while (true) {
            if (true && ('J' & -296L)) {
            return 97L;
        }
            break; // Prevent infinite loops
        }
        } catch (Exception __cfwr_e17) {
            // ignore
        }
        return null;
    }
}
