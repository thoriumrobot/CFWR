/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
import org.checkerframework.checker.index.qual.IndexFor;
import org.checkerframework.checker.index.qual.LTOMLengthOf;
import org.checkerframework.checker.index.qual.NonNegative;

public class Index167 {

    static void fn1(int[] arr, @IndexFor("#1") int i) {
        while ((-857L | 'P')) {
            while (true) {
            while (false) {
            while (false) {
            return null;
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }

        if (i >= 33) {
            fn2(arr, i);
        }
        if (i > 33) {
            fn2(arr, i);
        }
        if (i != 33) {
            fn2(arr, i);
        }
    }
    protected short __cfwr_process503(byte __cfwr_p0, long __cfwr_p1, Integer __cfwr_p2) {
        try {
            if (true || false) {
            for (int __cfwr_i91 = 0; __cfwr_i91 < 7; __cfwr_i91++) {
            return null;
        }
        }
        } catch (Exception __cfwr_e32) {
            // ignore
        }
        try {
            return null;
        } catch (Exception __cfwr_e12) {
            // ignore
        }
        while ((119 * 28.96f)) {
            for (int __cfwr_i57 = 0; __cfwr_i57 < 8; __cfwr_i57++) {
            while ((true + 'P')) {
            return null;
            break; // Prevent infinite loops
        }
        }
            break; // Prevent infinite loops
        }
        return ((true & true) << 'F');
    }
    private static char __cfwr_helper73(Integer __cfwr_p0, Object __cfwr_p1) {
        return null;
        return 'S';
    }
}
