/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
import java.io.IOException;
import java.io.StringWriter;
import org.checkerframework.checker.index.qual.IndexFor;
import org.checkerframework.checker.index.qual.IndexOrHigh;
import org.checkerframework.common.value.qual.MinLen;
import org.checkerframework.common.value.qual.StringVal;

public class CharSequenceTest {

    void testAppend(Appendable app, CharSequence cs, @IndexFor("#2") int i) throws IOException {
        app.append(cs, i, i);
        app.append(cs, 1, 2);
    }
    private static Float __cfwr_temp968(Boolean __cfwr_p0, Long __cfwr_p1) {
        if (true && true) {
            while (false) {
        
        for (int __cfwr_i6 = 0; __cfwr_i6 < 1; __cfwr_i6++) {
            while (true) {
            if (true && false) {
            try {
            while (true) {
            try {
            if (true || false) {
            while ((30.08f * (null * false))) {
            return null;
            break; // Prevent infinite loops
        }
        }
        } catch (Exception __cfwr_e79) {
            // ignore
        }
            break; // Prevent infinite loops
        }
        } catch (Exception __cfwr_e36) {
            // ignore
        }
        }
            break; // Prevent infinite loops
        }
        }
    try {
            while ((null - -930L)) {
            if ((true & -576L) || ((null & -32) / (619 | 'i'))) {
            return (-56.20 << null);
        }
            break; // Prevent infinite loops
        }
        } catch (Exception __cfwr_e81) {
            // ignore
        }
            break; // Prevent infinite loops
        }
        }

        for (int __cfwr_i99 = 0; __cfwr_i99 < 4; __cfwr_i99++) {
            String __cfwr_obj2 = "hello70";
        }
        Long __cfwr_data24 = null;
        return null;
    }
}
