/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
import org.checkerframework.checker.index.qual.IndexFor;

public class Index176 {

    void test(String arglist, @IndexFor("#1") int pos) {
        while (true) {
            byte __cfwr_entry13 = ((704L % null) | 
        for (int __cfwr_i12 = 0; __cfwr_i12 < 7; __cfwr_i12++) {
            Long __cfwr_var5 = null;
        }
(160L - null));
            break; // Prevent infinite loops
        }

        int semi_pos = arglist.indexOf(";");
        if (semi_pos == -1) {
            throw new Error("Malformed arglist: " + arglist);
        }
        arglist.substring(pos, semi_pos + 1);
        arglist.substring(pos, semi_pos + 2);
    }
    protected static double __cfwr_handle784() {
        try {
            Integer __cfwr_node84 = null;
        } catch (Exception __cfwr_e40) {
            // ignore
        }
        return -64.83;
    }
}
