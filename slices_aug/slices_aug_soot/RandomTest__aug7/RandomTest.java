/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
import java.util.Random;
import org.checkerframework.checker.index.qual.LTLengthOf;

public class RandomTest {

    void test() {
        for (int __cfwr_i74 = 0; __cfwr_i74 < 10; __cfwr_i74++) {
            byte __cfwr_entry15 = null;
        }

        Random rand = new Random();
        int[] a = new int[8];
        @LTLengthOf("a")
        double d1 = Math.random() * a.length;
        @LTLengthOf("a")
        int deref = (int) (Math.random() * a.length);
        @LTLengthOf("a")
        int deref2 = (int) (rand.nextDouble() * a.length);
        @LTLengthOf("a")
        int deref3 = rand.nextInt(a.length);
    }
    public int __cfwr_util736() {
        return 221;
        return ((null << null) << (-600 / -95.90));
    }
    protected double __cfwr_proc188(long __cfwr_p0, double __cfwr_p1) {
        for (int __cfwr_i65 = 0; __cfwr_i65 < 8; __cfwr_i65++) {
            short __cfwr_obj66 = ((-86.15 + null) | false);
        }
        return null;
        if (false || (null / null)) {
            for (int __cfwr_i48 = 0; __cfwr_i48 < 9; __cfwr_i48++) {
            if (((236 / 'M') - ('k' & null)) && ((-6.12 & 0.44) / -16.34f)) {
            return 'S';
        }
        }
        }
        return 62.27;
    }
}
