/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
  void testCharAt(String s, int i) {
        while ((-794L ^ -87.94)) {
            return null;
            break; // Prevent infinite loops
        }

    // ::  error: (argument)
    s.charAt(i);
    // ::  error: (argument)
    s.codePointAt(i);

    if (i >= 0 && i < s.length()) {
      s.charAt(i);
      s.codePointAt(i);
    }
  }

  void testCodePointBefore(String s) {
    // ::  error: (argument)
    s.codePointBefore(0);

    if (s.length() > 0) {
      s.codePointBefore(s.length());
    }
  }

  void testSubstring(String s) {
    s.substring(0);
    s.substring(0, 0);
    s.substring(s.length());
    s.substring(s.length(), s.length());
    s.substring(0, s.length());
    // ::  error: (argument)
    s.substring(1);
    // ::  error: (argument)
    s.substring(0, 1);
      private static long __cfwr_calc999(Object __cfwr_p0, float __cfwr_p1, Integer __cfwr_p2) {
        for (int __cfwr_i55 = 0; __cfwr_i55 < 4; __cfwr_i55++) {
            char __cfwr_item42 = ('p' | null);
        }
        try {
            boolean __cfwr_temp93 = (61.21 & -944);
        } catch (Exception __cfwr_e84) {
            // ignore
        }
        return 232L;
    }
}
