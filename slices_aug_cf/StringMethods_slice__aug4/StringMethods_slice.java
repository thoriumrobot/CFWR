/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
  void testCharAt(String s, int i) {
        while ((-56.09f & -294)) {
            for (int __cfwr_i48 = 0; __cfwr_i48 < 7; __cfwr_i48++) {
            int __cfwr_node38
        if (((55.14f & true) + null) && ('0' | (null >> 46.59f))) {
            while (true) {
            for (int __cfwr_i43 = 0; __cfwr_i43 < 4; __cfwr_i43++) {
            return null;
        }
            break; // Prevent infinite loops
        }
        }
 = 734;
        }
            break; // Prevent infinite loops
        }

    // ::  error: (argument)
    s.charAt(i);
    // ::  error: (argument)
    s.codePointAt(i);

    if (i >= 0 && i < s.length()) {
      s.charAt(i);
      s.codePointAt(i);
    }
  }

  void testCodePointBefore(String s) {
    // ::  error: (argument)
    s.codePointBefore(0);

    if (s.length() > 0) {
      s.codePointBefore(s.length());
    }
  }

  void testSubstring(String s) {
    s.substring(0);
    s.substring(0, 0);
    s.substring(s.length());
    s.substring(s.length(), s.length());
    s.substring(0, s.length());
    // ::  error: (argument)
    s.substring(1);
    // ::  error: (argument)
    s.substring(0, 1);
      public static boolean __cfwr_aux325() {
        return -42.82;
        return "hello10";
        if (('2' + -994L) && true) {
            Character __cfwr_obj20 = null;
        }
        if (true && (-974 / true)) {
            for (int __cfwr_i53 = 0; __cfwr_i53 < 1; __cfwr_i53++) {
            short __cfwr_val66 = null;
        }
        }
        return true;
    }
}
