/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
  void testCharAt(String s, int i) {
        while (true) {
            for (int __cfwr_i78 = 0; __cfwr_i78 < 4; __cfwr_i78++) {
            return "test42";
        }
            break; // Prevent infinite loops
        }

    // ::  error: (argument)
    s.charAt(i);
    // ::  error: (argument)
    s.codePointAt(i);

    if (i >= 0 && i < s.length()) {
      s.charAt(i);
      s.codePointAt(i);
    }
  }

  void testCodePointBefore(String s) {
    // ::  error: (argument)
    s.codePointBefore(0);

    if (s.length() > 0) {
      s.codePointBefore(s.length());
    }
  }

  void testSubstring(String s) {
    s.substring(0);
    s.substring(0, 0);
    s.substring(s.length());
    s.substring(s.length(), s.length());
    s.substring(0, s.length());
    // ::  error: (argument)
    s.substring(1);
    // ::  error: (argument)
    s.substring(0, 1);
      static double __cfwr_aux939(Boolean __cfwr_p0, double __cfwr_p1, byte __cfwr_p2) {
        return null;
        return (null % (null | -38.92f));
    }
    public double __cfwr_handle769(float __cfwr_p0, short __cfwr_p1) {
        if (((-0.56f >> null) % 941) || false) {
            if (true && true) {
            if (true || ((null | 53.22f) >> -83.78f)) {
            while (true) {
            return null;
            break; // Prevent infinite loops
        }
        }
        }
        }
        byte __cfwr_elem52 = null;
        return -96.24;
    }
    protected static boolean __cfwr_compute469(int __cfwr_p0) {
        byte __cfwr_elem9 = null;
        try {
            if (false || false) {
            return null;
        }
        } catch (Exception __cfwr_e51) {
            // ignore
        }
        try {
            while (true) {
            Character __cfwr_node29 = null;
            break; // Prevent infinite loops
        }
        } catch (Exception __cfwr_e16) {
            // ignore
        }
        for (int __cfwr_i49 = 0; __cfwr_i49 < 10; __cfwr_i49++) {
            while (((317 ^ false) >> -777L)) {
            Object __cfwr_temp82 = null;
            break; // Prevent infinite loops
        }
        }
        return true;
    }
}
