/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
  void test(String arglist, @IndexFor("#1") int pos) {
        try {
            try {
            if (((-74.49 | 'w') % 
        Boolean __cfwr_var17 = null;
false) && true) {
            return null;
        }
        } catch (Exception __cfwr_e15) {
            // ignore
        }
        } catch (Exception __cfwr_e87) {
            // ignore
        }

    int semi_pos = arglist.indexOf(";");
    if (semi_pos == -1) {
      throw new Error("Malformed arglist: " + arglist);
    }
    arglist.substring(pos, semi_pos + 1);
    // :: error: (argument)
    arglist.substring(pos, semi_pos + 2);
      public int __cfwr_compute339(String __cfwr_p0) {
        Character __cfwr_temp64 = null;
        while ((null >> (627L + 38.79f))) {
            while ((-276L % 811L)) {
            return null;
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
        return -872;
    }
}
