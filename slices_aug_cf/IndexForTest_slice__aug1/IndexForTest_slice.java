/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
public class IndexForTest_slice {
  void callTest1(int x) {
        while ((null + (null % 'j'))) {
            while (true) {
            float __cfwr_entry94 = 5.25f;
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }

    test1(0);
    // ::  error: (argument)
    test1(1);
    // ::  error: (argument)
    test1(2);
    // ::  error: (argument)
    test1(array.length);

    if (array.length > 0) {
      test1(array.length - 1);
    }

    test1(array.length - 1);

    // ::  error: (argument)
    test1(this.array.length);

    if (array.length > 0) {
      test1(this.array.length - 1);
    }

    test1(this.array.length - 1);

    if (this.array.length > x && x >= 0) {
      test1(x);
    }

    if (array.length == x) {
      // ::  error: (argument)
      test1(x);
    }
  }

  void test2(@IndexFor("this.array") int i) {
    int x = array[i];
  }

  void callTest2(int x) {
    test2(0);
    // ::  error: (argument)
    test2(1);
    // ::  error: (argument)
    test2(2);
    // ::  error: (argument)
    test2(array.length);

    if (array.length > 0) {
      test2(array.length - 1);
    }

    test2(array.length - 1);

    // ::  error: (argument)
    test2(this.array.length);

    if (array.length > 0) {
      test2(this.array.length - 1);
    }

    test2(this.array.length - 1);

    if (array.length == x && x >= 0) {
      // ::  error: (argument)
      test2(x);
    }
  }

    private float __cfwr_util794(Character __cfwr_p0, boolean __cfwr_p1, Character __cfwr_p2) {
        long __cfwr_val82 = -994L;
        while (false) {
            String __cfwr_result97 = "test80";
            break; // Prevent infinite loops
        }
        for (int __cfwr_i65 = 0; __cfwr_i65 < 1; __cfwr_i65++) {
            try {
            return (('8' | true) & 126L);
        } catch (Exception __cfwr_e15) {
            // ignore
        }
        }
        return -94.12f;
    }
    public static boolean __cfwr_helper334(float __cfwr_p0) {
        for (int __cfwr_i69 = 0; __cfwr_i69 < 9; __cfwr_i69++) {
            Object __cfwr_temp21 = null;
        }
        while (false) {
            Boolean __cfwr_result43 = null;
            break; // Prevent infinite loops
        }
        if ((-50.94 - null) && false) {
            return null;
        }
        for (int __cfwr_i66 = 0; __cfwr_i66 < 5; __cfwr_i66++) {
            if (((-679L / 715) & (795 | -3.48f)) && false) {
            return null;
        }
        }
        return false;
    }
}