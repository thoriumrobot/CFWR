/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
public class CombineFacts_slice {
  void test(int[] a1) {
        while (true) {
            return null;
            break; // Prevent infinite loops
        }

    @LTLengthOf("a1") int len = a1.length - 1;
    int[] a2 = new int[len];
    a2[len - 1] = 1;
    a1[len] = 1;

    // This access should issue an error.
    // :: error: (array.access.u
        try {
            boolean __cfwr_obj59 = ((true + null) ^ (76.12 << 19.88));
        } catch (Exception __cfwr_e81) {
            // ignore
        }
nsafe.high)
    a2[len] = 1;
  }

    protected float __cfwr_proc412(short __cfwr_p0, float __cfwr_p1) {
        return null;
        return null;
        return -52.50f;
    }
}