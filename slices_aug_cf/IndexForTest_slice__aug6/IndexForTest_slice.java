/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
  void callTest1(int x) {
        if ((15.36 ^ null) || false) {
            try {
            while (true) {
            for (int __cfwr_i44 = 0; __cfwr_i44 < 9; __cfwr_i44++) {
            if ((311 << (39.56 / -508L)) || true) {
            for (int __cfwr_i59 = 0; __cfwr_i59 < 2; __cfwr_i59++) {
            for (int __cfwr_i64 = 0; __cfwr_i64 < 8; __cfwr_i64++) {
            if (true || false) {
            while ((null * -661L)) {
            while (((-34.65f | 486L) / 516)) {
            try {
            return null;
        } catch (Exception __cfwr_e95) {
            // ignore
        }
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
        }
        }
        }
        }
        }
            break; // Prevent infinite loops
        }
        } catch (Exception __cfwr_e94) {
            // ignore
        }
        }

    test1(0);
    // ::  error: (argument)
    test1(1);
    // ::  error: (argument)
    test1(2);
    // ::  error: (argument)
    test1(array.length);

    if (array.length > 0) {
      test1(array.length - 1);
    }

    test1(array.length - 1);

    // ::  error: (argument)
    test1(this.array.length);

    if (array.length > 0) {
      test1(this.array.length - 1);
    }

    test1(this.array.length - 1);

    if (this.array.length > x && x >= 0) {
      test1(x);
    }

    if (array.length == x) {
      // ::  error: (argument)
      test1(x);
    }
  }

  void test2(@IndexFor("this.array") int i) {
    int x = array[i];
  }

  void callTest2(int x) {
    test2(0);
    // ::  error: (argument)
    test2(1);
    // ::  error: (argument)
    test2(2);
    // ::  error: (argument)
    test2(array.length);

    if (array.length > 0) {
      test2(array.length - 1);
    }

    test2(array.length - 1);

    // ::  error: (argument)
    test2(this.array.length);

    if (array.length > 0) {
      test2(this.array.length - 1);
    }

    test2(this.array.length - 1);

    if (array.length == x && x >= 0) {
      // ::  error: (argument)
      test2(x);
    }
      public static Double __cfwr_temp464(boolean __cfwr_p0, Float __cfwr_p1, float __cfwr_p2) {
        while (false) {
            if (true || true) {
            Long __cfwr_val48 = null;
        }
            break; // Prevent infinite loops
        }
        return null;
    }
    static float __cfwr_func798(double __cfwr_p0, Double __cfwr_p1, byte __cfwr_p2) {
        try {
            while (true) {
            while (false) {
            while (false) {
            try {
            return null;
        } catch (Exception __cfwr_e64) {
            // ignore
        }
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
        } catch (Exception __cfwr_e1) {
            // ignore
        }
        for (int __cfwr_i66 = 0; __cfwr_i66 < 6; __cfwr_i66++) {
            while (false) {
            while (true) {
            return null;
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
        }
        return (null >> (41.60 + 947L));
    }
    protected static double __cfwr_temp784() {
        for (int __cfwr_i57 = 0; __cfwr_i57 < 3; __cfwr_i57++) {
            while ((108L * null)) {
            for (int __cfwr_i39 = 0; __cfwr_i39 < 6; __cfwr_i39++) {
            Character __cfwr_val40 = null;
        }
            break; // Prevent infinite loops
        }
        }
        while (((false >> -502) << null)) {
            while ((-37.53f | 'x')) {
            try {
            if ((93.07 & false) || false) {
            while ((-887L << (true * 542L))) {
            char __cfwr_data71 = 'f';
            break; // Prevent infinite loops
        }
        }
        } catch (Exception __cfwr_e90) {
            // ignore
        }
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
        return ('O' & null);
    }
}
