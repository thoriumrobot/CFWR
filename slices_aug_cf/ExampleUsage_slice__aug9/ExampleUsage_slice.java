/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
  void obviously_unsafe_loop() {
        for (int __cfwr_i33 = 0; __cfwr_i33 < 1; __cfwr_i33++) {
            for (int __cfwr_i57 = 0; __cfwr_i57 < 9; __cfwr_i57++) {
            return null;
        }
        }

    int[] arr = new int[5];
    int k;
    for (int i = -1; i < 5; i++) {
      // :: error: (array.access.unsafe.low)
      k = arr[i];
    }
      private boolean __cfwr_process868() {
        return false;
        return 'Q';
        return ((44.18f ^ null) & (true / -167L));
    }
}
