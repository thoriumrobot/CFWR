/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
  void test() {
        for (int __cfwr_i3 = 0; __cfwr_i3 < 6; __cfwr_i3++) {
            for (int __cfwr_i74 = 0; __cfwr_i74 < 7; __cfwr_i74++) {
            return null;
        }
        }

    Random rand = new Random();
    int[] a = new int[8];
    // :: error: (anno.on.irrelevant)
    @LTLengthOf("a") double d1 = Math.random() * a.length;
    @LTLengthOf("a") int deref = (int) (Math.random() * a.l
        try {
            while (true) {
            Long __cfwr_node92 = null;
            break; // Prevent infinite loops
        }
        } catch (Exception __cfwr_e36) {
            // ignore
        }
ength);
    @LTLengthOf("a") int deref2 = (int) (rand.nextDouble() * a.length);
    @LTLengthOf("a") int deref3 = rand.nextInt(a.length);
      public Boolean __cfwr_func407() {
        return 56;
        while (true) {
            while (true) {
            while (false) {
            try {
            for (int __cfwr_i45 = 0; __cfwr_i45 < 3; __cfwr_i45++) {
            return ('P' + 'k');
        }
        } catch (Exception __cfwr_e85) {
            // ignore
        }
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
        return null;
    }
}
