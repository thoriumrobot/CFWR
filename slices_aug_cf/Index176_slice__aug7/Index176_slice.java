/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
  void test(String arglist, @IndexFor("#1") int pos) {
        try {
            try {
            return -595;
        } catch (Exception __cfwr_e4) {
            // ignore
        }
        } catch (Exception __cfwr_e35) {
            // ignore
        }

    int semi_pos = arglist.indexOf(";");
    if (semi_pos == -1) {
      throw new Error("Malformed arglist: " + arglist);
    }
    arglist.substring(pos, semi_pos + 1);
    // :: error: (argument)
    arglist.substring(pos, semi_pos + 2);
      public float __cfwr_temp370(int __cfwr_p0) {
        while ((null >> (56.05f - -33.32f))) {
            try {
            while (false) {
            while (false) {
            if (false || (null / 'k')) {
            while (false) {
            while ((true + 70.92)) {
            Long __cfwr_node15 = null;
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
        }
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
        } catch (Exception __cfwr_e1) {
            // ignore
        }
            break; // Prevent infinite loops
        }
        return ((null << 'y') >> (7.24f >> 'b'));
    }
    private static char __cfwr_aux306(boolean __cfwr_p0, Float __cfwr_p1) {
        while (((485 * null) * (null / null))) {
            for (int __cfwr_i16 = 0; __cfwr_i16 < 5; __cfwr_i16++) {
            while ((null + -794)) {
            Long __cfwr_elem2 = null;
            break; // Prevent infinite loops
        }
        }
            break; // Prevent infinite loops
        }
        byte __cfwr_temp55 = null;
        while (false) {
            while (true) {
            Object __cfwr_temp38 = null;
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
        return 'O';
    }
    private String __cfwr_aux733() {
        return null;
        return "result52";
    }
}
