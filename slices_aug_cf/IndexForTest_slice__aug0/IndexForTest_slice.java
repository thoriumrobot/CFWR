/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
public class IndexForTest_slice {
  void callTest1(int x) {
        while (false) {
            while (false) {
            for (int __cfwr_i44 = 0; __cfwr_i44 < 5; __cfwr_i44++) {
            byte __cfwr_node97 = ((null ^ true) & -883);
    
        Boolean __cfwr_val39 = null;
    }
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }

    test1(0);
    // ::  error: (argument)
    test1(1);
    // ::  error: (argument)
    test1(2);
    // ::  error: (argument)
    test1(array.length);

    if (array.length > 0) {
      test1(array.length - 1);
    }

    test1(array.length - 1);

    // ::  error: (argument)
    test1(this.array.length);

    if (array.length > 0) {
      test1(this.array.length - 1);
    }

    test1(this.array.length - 1);

    if (this.array.length > x && x >= 0) {
      test1(x);
    }

    if (array.length == x) {
      // ::  error: (argument)
      test1(x);
    }
  }

  void test2(@IndexFor("this.array") int i) {
    int x = array[i];
  }

  void callTest2(int x) {
    test2(0);
    // ::  error: (argument)
    test2(1);
    // ::  error: (argument)
    test2(2);
    // ::  error: (argument)
    test2(array.length);

    if (array.length > 0) {
      test2(array.length - 1);
    }

    test2(array.length - 1);

    // ::  error: (argument)
    test2(this.array.length);

    if (array.length > 0) {
      test2(this.array.length - 1);
    }

    test2(this.array.length - 1);

    if (array.length == x && x >= 0) {
      // ::  error: (argument)
      test2(x);
    }
  }

    byte __cfwr_util427(char __cfwr_p0, boolean __cfwr_p1, Float __cfwr_p2) {
        if (true && true) {
            return null;
        }
        return null;
    }
    String __cfwr_calc558(boolean __cfwr_p0) {
        if (false && ((false + 158L) / -751)) {
            try {
            for (int __cfwr_i51 = 0; __cfwr_i51 < 2; __cfwr_i51++) {
            while (false) {
            try {
            while ((-837L * -49.21f)) {
            try {
            while (false) {
            try {
            boolean __cfwr_temp11 = false;
        } catch (Exception __cfwr_e36) {
            // ignore
        }
            break; // Prevent infinite loops
        }
        } catch (Exception __cfwr_e94) {
            // ignore
        }
            break; // Prevent infinite loops
        }
        } catch (Exception __cfwr_e86) {
            // ignore
        }
            break; // Prevent infinite loops
        }
        }
        } catch (Exception __cfwr_e7) {
            // ignore
        }
        }
        return "world5";
    }
    private byte __cfwr_proc987(Long __cfwr_p0, Float __cfwr_p1) {
        return null;
        if (('w' % true) || true) {
            if ((('c' / -336) >> (537L | null)) || ((41.24f * -54.46f) / 'C')) {
            Long __cfwr_item15 = null;
        }
        }
        try {
            return null;
        } catch (Exception __cfwr_e32) {
            // ignore
        }
        boolean __cfwr_val8 = (true * (-122L % true));
        return null;
    }
}