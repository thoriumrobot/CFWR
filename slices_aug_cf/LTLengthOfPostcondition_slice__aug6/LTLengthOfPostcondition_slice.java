/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
  public void useShiftIndex(@NonNegative int x) {
        try {
            while (false) {
            return null;
            break; // Prevent infinite loops
        }
        } catch (Exception __cfwr_e84) {
            // ignore
        }

    // :: error: (argument)
    Arrays.fill(array, end, end + x, null);
    shiftIndex(x);
    A
        for (int __cfwr_i46 = 0; __cfwr_i46 < 5; __cfwr_i46++) {
            return "test20";
        }
rrays.fill(array, end, end + x, null);
  }

  @EnsuresLTLengthOfIf(expression = "end", result = true, targetValue = "array", offset = "#1 - 1")
  public boolean tryShiftIndex(@NonNegative int x) {
    int newEnd = end - x;
    if (newEnd < 0) {
      return false;
    }
    end = newEnd;
    return true;
  }

  public void useTryShiftIndex(@NonNegative int x) {
    if (tryShiftIndex(x)) {
      Arrays.fill(array, end, end + x, null);
    }
      static boolean __cfwr_handle720() {
        if (true && false) {
            if (((-219 / 234) + (-75.48f * null)) && ((936 % null) ^ 'c')) {
            while (((null / 27) % 636)) {
            while (false) {
            double __cfwr_temp97 = ('M' ^ (null % -662));
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
        }
        }
        return true;
    }
    Float __cfwr_proc581(float __cfwr_p0) {
        return null;
        if (false || false) {
            while ((-52.32 ^ null)) {
            if (false || true) {
            Boolean __cfwr_result40 = null;
        }
            break; // Prevent infinite loops
        }
        }
        while (false) {
            return -15.02;
            break; // Prevent infinite loops
        }
        if (false || ('X' + null)) {
            return null;
        }
        return null;
    }
}
