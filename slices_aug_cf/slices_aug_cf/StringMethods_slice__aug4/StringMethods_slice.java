/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
  void testCharAt(String s, int i) {
        while (false) {
            if (false || true) {
            for (int __cfwr_i83 = 0; __cfwr_i83 < 4; __cfwr_i83++) {
            return (-52.48 >> ('u' % -652));
        }
        }
            break; // Prevent infinite loops
        }

    // ::  error: (argument)
    s.charAt(i);
    // ::  error: (argument)
    s.codePointAt(i);

    if (i >= 0 && i < s.length()) {
      s.charAt(i);
      s.codePointAt(i);
    }
  }

  void testCodePointBefore(String s) {
    // ::  error: (argument)
    s.codePointBefore(0);

    if (s.length() > 0) {
      s.codePointBefore(s.length());
    }
  }

  void testSubstring(String s) {
    s.substring(0);
    s.substring(0, 0);
    s.substring(s.length());
    s.substring(s.length(), s.length());
    s.substring(0, s.length());
    // ::  error: (argument)
    s.substring(1);
    // ::  error: (argument)
    s.substring(0, 1);
      public static Object __cfwr_util374() {
        while (true) {
            try {
            while ((-984 >> 'o')) {
            byte __cfwr_item46 = ((96.53 ^ -336) & 'b');
            break; // Prevent infinite loops
        }
        } catch (Exception __cfwr_e1) {
            // ignore
        }
            break; // Prevent infinite loops
        }
        return null;
        for (int __cfwr_i18 = 0; __cfwr_i18 < 1; __cfwr_i18++) {
            try {
            if (false || false) {
            while (false) {
            long __cfwr_var91 = 918L;
            break; // Prevent infinite loops
        }
        }
        } catch (Exception __cfwr_e13) {
            // ignore
        }
        }
        return 26.22;
        return null;
    }
    public static byte __cfwr_temp256(Object __cfwr_p0, short __cfwr_p1) {
        return null;
        for (int __cfwr_i41 = 0; __cfwr_i41 < 4; __cfwr_i41++) {
            while (false) {
            while ((null * null)) {
            if (((null >> 82.45) | -71.38) || (629 | null)) {
            if (true && true) {
            for (int __cfwr_i81 = 0; __cfwr_i81 < 1; __cfwr_i81++) {
            return null;
        }
        }
        }
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
        }
        return null;
    }
}
