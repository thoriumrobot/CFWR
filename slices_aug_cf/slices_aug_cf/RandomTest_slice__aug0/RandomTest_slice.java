/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
  void test() {
        if ((null - true) && (434L << -24.78)) {
            try {
            while (false) {
            if (true || true) {
            if (true || false) {
            while (false) {
            double __cfwr_val75 = 36.09;
            break; // Prevent infinite loops
        }
        }
        }
            break; // Prevent infinite loops
        }
        } catch (Exception __cfwr_e32) {
            // ignore
        }
        }

    Random rand = new Random();
    int[] a = new int[8];
    // :: error: (anno.on.irrelevant)
    @LTLengthOf("a") double d1 = Math.random() * a.length;
    @LTLengthOf("a") int deref = (int) (Math.random() * a.length);
    @LTLengthOf("a") int deref2 = (int) (rand.nextDouble() * a.length);
    @LTLengthOf("a") int deref3 = rand.nextInt(a.length);
      public Boolean __cfwr_compute132(int __cfwr_p0, byte __cfwr_p1, byte __cfwr_p2) {
        while (true) {
            while ((759 >> (-908L / null))) {
            int __cfwr_temp32 = 776;
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
        Float __cfwr_item17 = null;
        return null;
    }
}
