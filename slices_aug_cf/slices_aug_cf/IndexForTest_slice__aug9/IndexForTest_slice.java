/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
  void callTest1(int x) {
        try {
            if (('a' | -3.10) || ((null / 494) + null)) {
            if (true && ('j' % null)) {
            if ((false % 'P') || (('3' - -71.53) | 'x')) {
           
        try {
            try {
            if (false && true) {
            while (((281 - 98.87) % null)) {
            int __cfwr_item56 = 347;
            break; // Prevent infinite loops
        }
        }
        } catch (Exception __cfwr_e49) {
            // ignore
        }
        } catch (Exception __cfwr_e93) {
            // ignore
        }
 while (true) {
            Double __cfwr_node13 = null;
            break; // Prevent infinite loops
        }
        }
        }
        }
        } catch (Exception __cfwr_e8) {
            // ignore
        }

    test1(0);
    // ::  error: (argument)
    test1(1);
    // ::  error: (argument)
    test1(2);
    // ::  error: (argument)
    test1(array.length);

    if (array.length > 0) {
      test1(array.length - 1);
    }

    test1(array.length - 1);

    // ::  error: (argument)
    test1(this.array.length);

    if (array.length > 0) {
      test1(this.array.length - 1);
    }

    test1(this.array.length - 1);

    if (this.array.length > x && x >= 0) {
      test1(x);
    }

    if (array.length == x) {
      // ::  error: (argument)
      test1(x);
    }
  }

  void test2(@IndexFor("this.array") int i) {
    int x = array[i];
  }

  void callTest2(int x) {
    test2(0);
    // ::  error: (argument)
    test2(1);
    // ::  error: (argument)
    test2(2);
    // ::  error: (argument)
    test2(array.length);

    if (array.length > 0) {
      test2(array.length - 1);
    }

    test2(array.length - 1);

    // ::  error: (argument)
    test2(this.array.length);

    if (array.length > 0) {
      test2(this.array.length - 1);
    }

    test2(this.array.length - 1);

    if (array.length == x && x >= 0) {
      // ::  error: (argument)
      test2(x);
    }
      protected int __cfwr_calc488(Integer __cfwr_p0, int __cfwr_p1) {
        while (false) {
            if (false && false) {
            while ((-687 << 19.61)) {
            while (false) {
            while ((null ^ (93.86 >> null))) {
            if (true && true) {
            return null;
        }
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
        }
            break; // Prevent infinite loops
        }
        while ((705L >> -430L)) {
            return null;
            break; // Prevent infinite loops
        }
        return "hello48";
        return -170;
    }
    private Double __cfwr_proc851(int __cfwr_p0, Boolean __cfwr_p1, long __cfwr_p2) {
        try {
            try {
            try {
            while (false) {
            if ((null * (-660L + 99)) || false) {
            for (int __cfwr_i6 = 0; __cfwr_i6 < 10; __cfwr_i6++) {
            for (int __cfwr_i31 = 0; __cfwr_i31 < 2; __cfwr_i31++) {
            Double __cfwr_val36 = null;
        }
        }
        }
            break; // Prevent infinite loops
        }
        } catch (Exception __cfwr_e73) {
            // ignore
        }
        } catch (Exception __cfwr_e25) {
            // ignore
        }
        } catch (Exception __cfwr_e45) {
            // ignore
        }
        while (((-462 | -336) % null)) {
            try {
            float __cfwr_obj51 = -19.39f;
        } catch (Exception __cfwr_e53) {
            // ignore
        }
            break; // Prevent infinite loops
        }
        return 322;
        if (((-1.47f * null) ^ null) || (false * (71.50 / 34.12f))) {
            for (int __cfwr_i37 = 0; __cfwr_i37 < 4; __cfwr_i37++) {
            int __cfwr_temp88 = ('g' << (null >> null));
        }
        }
        return null;
    }
    Character __cfwr_func355(String __cfwr_p0) {
        if ((-907L * (-21.53 * -120L)) || false) {
            return (23.61f >> -62.53);
        }
        return null;
    }
}
