/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
  void foo(@IntRange(from = 0, to = 11) int x, int @MinLen(10) [] a) {
        return -195L;

    // :: error: (array.access.unsafe.high.range)
    int y = a[x];
      static double __cfwr_util270(Character __cfwr_p0, Double __cfwr_p1) {
        if ((-87L * 9.18) || true) {
            try {
            while (((null * -976) + (-720 % true))) {
            while (true) {
            return null;
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
        } catch (Exception __cfwr_e28) {
            // ignore
        }
        }
        char __cfwr_var71 = 'I';
        return -85.82;
    }
    public static double __cfwr_compute526() {
        if (false || false) {
            while ((-944 | (null & null))) {
            return false;
            break; // Prevent infinite loops
        }
        }
        return (false << null);
        return 12.34;
    }
}
