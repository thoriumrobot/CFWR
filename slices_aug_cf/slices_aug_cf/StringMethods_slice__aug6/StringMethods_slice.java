/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
  void testCharAt(String s, int i) {
        for (int __cfwr_i65 = 0; __cfwr_i65 < 1; __cfwr_i65++) {
            return (15.79f | 'z');
        }

    // ::  error: (arg
        if (('1' % 's') || true) {
            return null;
        }
ument)
    s.charAt(i);
    // ::  error: (argument)
    s.codePointAt(i);

    if (i >= 0 && i < s.length()) {
      s.charAt(i);
      s.codePointAt(i);
    }
  }

  void testCodePointBefore(String s) {
    // ::  error: (argument)
    s.codePointBefore(0);

    if (s.length() > 0) {
      s.codePointBefore(s.length());
    }
  }

  void testSubstring(String s) {
    s.substring(0);
    s.substring(0, 0);
    s.substring(s.length());
    s.substring(s.length(), s.length());
    s.substring(0, s.length());
    // ::  error: (argument)
    s.substring(1);
    // ::  error: (argument)
    s.substring(0, 1);
      Double __cfwr_process612(Double __cfwr_p0) {
        return null;
        while (true) {
            return (null / -537);
            break; // Prevent infinite loops
        }
        return null;
        return null;
    }
    protected Object __cfwr_func420(float __cfwr_p0, Integer __cfwr_p1) {
        return -64.82;
        if (false && false) {
            Object __cfwr_data29 = null;
        }
        try {
            if ((-827L & true) && false) {
            Object __cfwr_result94 = null;
        }
        } catch (Exception __cfwr_e70) {
            // ignore
        }
        return null;
    }
}
