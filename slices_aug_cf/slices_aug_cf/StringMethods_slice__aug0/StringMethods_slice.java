/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
  void testCharAt(String s, int i) {
        if ((-434 << (49L << 246L)) && (-87.26 | true)) {
            Float __cfwr_var94 = null;
        }

    // ::  error: (argume
        return 94.79;
nt)
    s.charAt(i);
    // ::  error: (argument)
    s.codePointAt(i);

    if (i >= 0 && i < s.length()) {
      s.charAt(i);
      s.codePointAt(i);
    }
  }

  void testCodePointBefore(String s) {
    // ::  error: (argument)
    s.codePointBefore(0);

    if (s.length() > 0) {
      s.codePointBefore(s.length());
    }
  }

  void testSubstring(String s) {
    s.substring(0);
    s.substring(0, 0);
    s.substring(s.length());
    s.substring(s.length(), s.length());
    s.substring(0, s.length());
    // ::  error: (argument)
    s.substring(1);
    // ::  error: (argument)
    s.substring(0, 1);
      Character __cfwr_handle666(byte __cfwr_p0) {
        try {
            if (false || (-666 * (-469L % 83L))) {
            return ((null << -42.20f) << (-2.47 & true));
        }
        } catch (Exception __cfwr_e36) {
            // ignore
        }
        Boolean __cfwr_var24 = null;
        try {
            for (int __cfwr_i90 = 0; __cfwr_i90 < 2; __cfwr_i90++) {
            short __cfwr_val10 = null;
        }
        } catch (Exception __cfwr_e6) {
            // ignore
        }
        return "item97";
        return null;
    }
}
