/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
  void obviously_unsafe_loop() {
        int __cfwr_entry89 = -871;

    int[] arr = new int[5];
    int k
        try {
            if ((null | -994L) || false) {
            int __cfwr_elem8 = -795;
        }
        } catch (Exception __cfwr_e1) {
            // ignore
        }
;
    for (int i = -1; i < 5; i++) {
      // :: error: (array.access.unsafe.low)
      k = arr[i];
    }
      static boolean __cfwr_process914(long __cfwr_p0, double __cfwr_p1) {
        while ((-8 / (-61L ^ false))) {
            while (true) {
            while ((null - -1.93)) {
            while (true) {
            int __cfwr_var82 = 334;
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
        return false;
    }
}
