/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
  void testCharAt(String s, int i) {
        for (int __cfwr_i50 = 0; __cfwr_i50 < 3; __cfwr_i50++) {
            try {
            while (true) {
            if (true &&
        for (int __cfwr_i63 = 0; __cfwr_i63 < 2; __cfwr_i63++) {
            while (true) {
            Character __cfwr_elem12 = null;
            break; // Prevent infinite loops
        }
        }
 false) {
            for (int __cfwr_i40 = 0; __cfwr_i40 < 7; __cfwr_i40++) {
            try {
            Object __cfwr_node68 = null;
        } catch (Exception __cfwr_e51) {
            // ignore
        }
        }
        }
            break; // Prevent infinite loops
        }
        } catch (Exception __cfwr_e26) {
            // ignore
        }
        }

    // ::  error: (argument)
    s.charAt(i);
    // ::  error: (argument)
    s.codePointAt(i);

    if (i >= 0 && i < s.length()) {
      s.charAt(i);
      s.codePointAt(i);
    }
  }

  void testCodePointBefore(String s) {
    // ::  error: (argument)
    s.codePointBefore(0);

    if (s.length() > 0) {
      s.codePointBefore(s.length());
    }
  }

  void testSubstring(String s) {
    s.substring(0);
    s.substring(0, 0);
    s.substring(s.length());
    s.substring(s.length(), s.length());
    s.substring(0, s.length());
    // ::  error: (argument)
    s.substring(1);
    // ::  error: (argument)
    s.substring(0, 1);
      Integer __cfwr_process396() {
        if (false && false) {
            try {
            try {
            while (true) {
            if (true || false) {
            for (int __cfwr_i41 = 0; __cfwr_i41 < 3; __cfwr_i41++) {
            if ((('X' * 696L) / -872) || false) {
            while ((-783 >> true)) {
            while ((33.99f - (-7.80f | 'r'))) {
            try {
            return null;
        } catch (Exception __cfwr_e99) {
            // ignore
        }
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
        }
        }
        }
            break; // Prevent infinite loops
        }
        } catch (Exception __cfwr_e91) {
            // ignore
        }
        } catch (Exception __cfwr_e94) {
            // ignore
        }
        }
        while (true) {
            while (true) {
            return null;
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
        while (true) {
            while ((-753L * null)) {
            for (int __cfwr_i26 = 0; __cfwr_i26 < 3; __cfwr_i26++) {
            while (true) {
            while ((('l' & 167) * 'N')) {
            int __cfwr_data8 = (758 / (162 >> null));
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
        }
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
        return null;
    }
    public int __cfwr_aux616() {
        return -28;
        Boolean __cfwr_item77 = null;
        if (('k' + (false | -70.23f)) || true) {
            Object __cfwr_item14 = null;
        }
        if (true && (-553 / -214L)) {
            return (-79.92 ^ -42.45f);
        }
        return -421;
    }
}
