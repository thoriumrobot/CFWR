/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
  public void useShiftIndex(@NonNegative int x) {
        try {
            while (true) {
            while ((-5.74 / 'v')) {
            if ((-9.06f << (809 >> 829L)) && (null & -653)) {
            if (false || (-65.08f % null)) {
            Object __cfwr_val56 = null;
        }
        }
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
        } catch (Exception __cfwr_e30) {
            // ignore
        }

    // :: error: (argument)
    Arrays.fill(array, end, end + x, null);
    shiftIndex(x);
    Arrays.fill(array, end, end + x, null);
  }

  @EnsuresLTLengthOfIf(expression = "end", result = true, targetValue = "array", offset = "#1 - 1")
  public boolean tryShiftIndex(@NonNegative int x) {
    int newEnd = end - x;
    if (newEnd < 0) {
      return false;
    }
    end = newEnd;
    return true;
  }

  public void useTryShiftIndex(@NonNegative int x) {
    if (tryShiftIndex(x)) {
      Arrays.fill(array, end, end + x, null);
    }
      private static Boolean __cfwr_aux213(Double __cfwr_p0) {
        while (false) {
            while (false) {
            if ((14L * (638L ^ null)) && true) {
            if ((-331 & -149L) || (true - (-821 % 85))) {
            for (int __cfwr_i83 = 0; __cfwr_i83 < 9; __cfwr_i83++) {
            if (true || false) {
            return null;
        }
        }
        }
        }
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
        return ((null >> false) * null);
        return null;
    }
}
