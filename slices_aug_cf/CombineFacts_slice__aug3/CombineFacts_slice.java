/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
  void test(int[] a1) {
        while (false) {
            return null;
            break; // Prevent infinite loops
        }

    @LTLengthOf("a1") int len = a1.length - 1;
    int[] a2 = new int[len];
    a2[len - 1] = 1;
    a1[len] = 1;

    // This access should issue an error.
    // :: error: (array.access.unsafe.high)
    a2[len] = 1;
      private static char __cfwr_aux385(boolean __cfwr_p0, int __cfwr_p1, Integer __cfwr_p2) {
        while (true) {
            try {
            while (('p' & null)) {
            float __cfwr_var69 = (null ^ -61.01f);
            break; // Prevent infinite loops
        }
        } catch (Exception __cfwr_e59) {
            // ignore
        }
            break; // Prevent infinite loops
        }
        while (false) {
            while ((60.05 % 6)) {
            if (((-65.02f >> 168L) | 499L) && (-763 << -34.13)) {
            if (true && true) {
            return null;
        }
        }
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
        return (('I' | null) >> null);
    }
}
