/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
  void callTest1(int x) {
        byte __cfwr_result91 = null;

    test1(0);
    // ::  error: (argument)
    test1(1);
    // ::  error: (argument)
    test1(2);
    // ::  error: (argument)
    test1(array
        try {
            for (int __cfwr_i8 = 0; __cfwr_i8 < 7; __cfwr_i8++) {
            while (false) {
            long __cfwr_obj14 = 757L;
            break; // Prevent infinite loops
        }
        }
        } catch (Exception __cfwr_e74) {
            // ignore
        }
.length);

    if (array.length > 0) {
      test1(array.length - 1);
    }

    test1(array.length - 1);

    // ::  error: (argument)
    test1(this.array.length);

    if (array.length > 0) {
      test1(this.array.length - 1);
    }

    test1(this.array.length - 1);

    if (this.array.length > x && x >= 0) {
      test1(x);
    }

    if (array.length == x) {
      // ::  error: (argument)
      test1(x);
    }
  }

  void test2(@IndexFor("this.array") int i) {
    int x = array[i];
  }

  void callTest2(int x) {
    test2(0);
    // ::  error: (argument)
    test2(1);
    // ::  error: (argument)
    test2(2);
    // ::  error: (argument)
    test2(array.length);

    if (array.length > 0) {
      test2(array.length - 1);
    }

    test2(array.length - 1);

    // ::  error: (argument)
    test2(this.array.length);

    if (array.length > 0) {
      test2(this.array.length - 1);
    }

    test2(this.array.length - 1);

    if (array.length == x && x >= 0) {
      // ::  error: (argument)
      test2(x);
    }
      public Integer __cfwr_compute174(int __cfwr_p0, byte __cfwr_p1) {
        float __cfwr_entry79 = -53.78f;
        boolean __cfwr_elem66 = (null ^ -149);
        if (true || true) {
            while (false) {
            try {
            if ((('j' / -280) * 37.33f) || true) {
            Long __cfwr_node4 = null;
        }
        } catch (Exception __cfwr_e56) {
            // ignore
        }
            break; // Prevent infinite loops
        }
        }
        return null;
    }
    static Long __cfwr_proc847(byte __cfwr_p0, char __cfwr_p1, Character __cfwr_p2) {
        Long __cfwr_obj36 = null;
        if (false && true) {
            while (true) {
            try {
            try {
            return null;
        } catch (Exception __cfwr_e81) {
            // ignore
        }
        } catch (Exception __cfwr_e34) {
            // ignore
        }
            break; // Prevent infinite loops
        }
        }
        while (false) {
            if ((null >> ('L' + null)) || false) {
            if (true && (177L % -62.47f)) {
            try {
            for (int __cfwr_i89 = 0; __cfwr_i89 < 8; __cfwr_i89++) {
            return null;
        }
        } catch (Exception __cfwr_e74) {
            // ignore
        }
        }
        }
            break; // Prevent infinite loops
        }
        return ((null + null) - 577L);
        return null;
    }
}
